[package]
name = "knocode-workflow"
version = "0.6.0"
edition = "2021"
description = "Knocode workflow (future, DBOS) — preserved out of v1 hot path, opt-in via future/workflow"
license = "MIT"

[dependencies]
knocode-core = { path = "../../crates/knocode-core" }
knocode-storage = { path = "../../crates/knocode-storage" }
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
anyhow = "1"
tracing = "0.1"
reqwest = { version = "0.12", features = ["json"] }
uuid = { version = "1", features = ["v4"] }
chrono = { version = "0.4", features = ["serde"] }
sha2 = "0.10"
hmac = "0.12"
async-trait = "0.1"
axum = "0.7"
